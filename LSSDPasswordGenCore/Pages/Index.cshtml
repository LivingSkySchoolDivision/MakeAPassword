@page
@model LSSDPasswordGenCore.Pages.IndexModel
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <style type="text/css">
        body {
            font-family: sans-serif;
        }

        .reload_message {
            width: 100%; 
            text-align: center; 
            font-size: 9pt; 
            font-weight: bold;
            padding: 5px;
            border: 1px solid rgba(0,0,0,0.3);
            margin-top: 10px;
        }

        .control_table {
            width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        #page_container {
            width: 800px;
            margin-left: auto;
            margin-right: auto;
        }

        #logo_container {
            margin: 25px;
            text-align: center;
        }

        #lskysd_logo {
            width: 200px;
            margin-left: auto;
            margin-right: auto;
        }

        .caution-icon {
            vertical-align: middle;
        }

        li {
            list-style-type: square;
            margin-bottom: 10px;
        }

        .LoginFormContainer {
            margin-left: auto;
            margin-right: auto;
            text-align: right;
        }

        .already_active_text {
            font-size: 8pt;
        }

        .usernameandpassword {
            font-size: 14pt;
        }

        .credential_table {
            width: 100%;
        }

        .usernameandpassword_title {
            text-transform: uppercase;
            font-size: 10pt;
            font-weight: bold;
        }

        .usernameandpassword {
            padding: 10px;
            border: 2px solid #C0C0C0;
            border-radius: 5px;
            text-align: center;
            font-family: serif;
        }

        .input_error {
            border: 1px solid red;
        }

        .smalltext {
            font-size: 8pt;
        }

        .footer {
            font-size: 6pt;
            text-align: center;
        }
    </style>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>

    <div id="page_container">
        <h1>Passwords you can memorize</h1>

        <table style="width:100%;">
            <tr>
                <td>
                    <div class="usernameandpassword">
                        <div id="lblWordBasedSimple">
                            @foreach (string pwd in Model.PasswordsWordsLow)
                            {
                                <div>@pwd</div>
                            }
                        </div>
                    </div>
                </td>
                <td>
                    <div class="usernameandpassword">
                        <div id="lblWordBasedMedium">
                            @foreach (string pwd in Model.PasswordsWordsMedium)
                            {
                                <div>@pwd</div>
                            }
                        </div>
                    </div>
                </td>
                <td>
                    <div class="usernameandpassword">
                        @foreach (string pwd in Model.PasswordsWordsHigh)
                        {
                            <div>@pwd</div>
                        }
                        <span id="lblWordBasedLong">
                        </span>
                    </div>
                </td>
            </tr>
        </table><div class="reload_message">Reload the page to generate more</div>
        <h2>How to use memorable passwords</h2>
        <p>The idea behind these passwords is that <b>longer</b> passwords <b>that you can remember</b> are better than shorter passwords that are harder to remember.</p>
        <p>Pick a password from the above list, and make up a sentence or a story with those words that you can remember. Combine several together to make an extremely hard to guess password.</p>
        <p>To increase security, capitalize one or more words, add special characters, or change the order of the words.</p>
        <br /><br />
        <h1>Complex passwords for password managers</h1>
        <asp:TableCell>
            <div class="usernameandpassword" style="font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif; font-size: 10pt;">
                <div id="lblComplexPasswords">
                    @foreach (string pwd in Model.PasswordsComplexLow)
                    {
                        <div>@pwd</div>
                    }
                    @foreach (string pwd in Model.PasswordsComplexHigh)
                    {
                        <div>@pwd</div>
                    }
                </div>
            </div>
        </asp:TableCell>
        <h2>How to use complex passwords</h2>
        <p>Use these with a password manager system, such as <a href="https://www.lastpass.com/">LastPass</a>, or <a href="https://keepass.info/">KeePass</a>. Don't try to memorize these, they are meant to be either copied and pasted, or automatically submitted via a password manager program or browser add-in.</p>
        <br /><br />
        <h1>Complex passwords for YubiKeys</h1>
        <p>Yubikey static passwords are limited to 32 characters.</p>
        <table style="width:100%;">
            <tr>
                <td>
                    <div class="usernameandpassword" style="font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif; font-size: 10pt;">
                        <div id="lblYubikeyPasswords_alpha">
                            @foreach (string pwd in Model.PasswordsYubikeyLow)
                            {
                                <div>@pwd</div>
                            }
                        </div>
                    </div>
                </td>
                <td>
                    <div class="usernameandpassword" style="font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif; font-size: 10pt;">
                        <div id="lblYubikeyPasswords_special">
                            @foreach (string pwd in Model.PasswordsYubikeyHigh)
                            {
                                <div>@pwd</div>
                            }
                        </div>
                    </div>
                </td>
            </tr>
        </table>
        <br /><br />
        <div class="footer">Passwords generated in @Model.stopwatch.Elapsed</div>
        <br /><br /><br />
    </div>
</body>
</html>
